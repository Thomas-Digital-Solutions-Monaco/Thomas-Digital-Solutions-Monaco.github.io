// Minimal JS. As an ES module, this is already Vite-ready:
// when you migrate, this same file can `import` components/styles.
document.getElementById("year").textContent = new Date().getFullYear();
